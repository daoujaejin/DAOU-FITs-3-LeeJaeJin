# 문제

- [x]  db.c에 get 함수들 사용하면 ‘Error: 000000F0D29FF4B0’ 출력되는 현상
- [x]  DB에서 데이터 받아올 때 문자열 ???로 되는 현상
- [x]  oracle database connected successfully 출력되는 현상
- [x]  챗봇 대화 들어가면 `질문을 입력해주세요` 가 두 번 나옴
    - 상단에 getchar() 추가
- [x]  USERSTOCK 조회 문제 (예상)
    
    ![image.png](attachment:3d856efe-c901-484e-a7c2-153798c33b7f:image.png)
    
- [x]  완전사소한 줄바꿈 이슈..
    
    ```c
    등록된 id가 없습니다???) ID와 일치하는 회원 정보가 없습니다.
    ```
    
    지우겠습니다ㅋㅋ
    
- [x]  db_getUserStock 반환값에서 quantity

# 구현

- `int db_insertUserChat(chat new_chat)`
    - insert user_id, title, summary, content
        
        ```c
        create table CHAT
        (
            CHAT_ID    NUMBER       default "C##C_CHATBOT"."ISEQ$$_84335".nextval generated as identity
        		primary key,
            USER_ID    VARCHAR2(255),
            TITLE      VARCHAR2(255),
            SUMMARY    CLOB,
            CONTENT    CLOB,
            CREATED_AT TIMESTAMP(6) default SYSTIMESTAMP,
            UPDATED_AT TIMESTAMP(6) default SYSTIMESTAMP,
            DELETED_AT TIMESTAMP(6) default NULL
        )
        ```
        
- `Chat* db_getUserChats(char *user_id, int *chat_count)`
    
    <aside>
    🤖
    
    채팅 없으면 NULL 출력
    
    </aside>
    
    ```c
    typedef struct {
        int chat_id;
        char *user_id;
        char *title;
        char *summary;
        char *content;
        char *created_at;
        char *updated_at;
        char *deleted_at;
    } Chat;
    ```
    
    - 채팅수
        - SELECT count(*)
        from CHAT
        where USER_ID='daou';
    - 채팅리스트 메모리 할당
        
        `malloc(sizeof(Chat)*(*outCount));`
        
    - chat list select
        - SELECT *
        from CHAT
        where USER_ID='daou';
- `void free_asset(Asset *asset);`
    
    → Asset 구조체 포인터 할당 해제할 때는 `free()` 대신 이 함수 이용하기
    
    위치: asset.h
    
- `db_getUserStockList` 호출하는 부분 수정
    
    ```c
    typedef union {
        struct {
            User_Stock *user_stock;
            int stock_count;
        } stock;
    
    } Asset_Data;
    ```
    
    총 주식 개수 stock_count에 저장하도록 수정
    
    `Asset* db_getUserAsset(const char* user_id)`
    
    ```c
    // --------------------------
        // (C) 보유 주식 목록 조회
        // --------------------------
        int stockCount = 0;
        User_Stock *stockArray = db_getUserStockList(user_id, &stockCount);
        // 여기서 stockCount에는 "몇 개의 주식 종목인지"가 들어감
    
        // 만약 하나도 없으면 stockArray == NULL
        if (stockArray) {
            assets[IDX_STOCK].data.stock.user_stock = stockArray;
            assets[IDX_STOCK].data.stock.stock_count = stockCount;
        } else {
            assets[IDX_STOCK].data.stock.user_stock = NULL;
            assets[IDX_STOCK].data.stock.stock_count = 0;
            // count=0 => 주식 없음
        }
    
    ```
    

```c
    const char *sql_user_stocks =
        "SELECT us.USER_STOCK_ID, us.STOCK_ID, s.NAME, us.QUANTITY, us.TOTAL_PRICE, s.PRICE FROM USER_STOCK us, STOCK s WHERE us.USER_ID = :1 AND us.STOCK_ID = s.STOCK_ID";

```

```c
Message system_messages[] = {
    {
        "system",
        "Please provide a plain text response without any formatting or emphasis. Use simple language and avoid idiomatic expressions. And answer by Korean."
    },
    {
        "system",
        "너는 지금부터 고객 자금 관리 시스템을 가지고 상담원 역할을 할거야."
    },
    {
        "system",
        "유저의 질문에 대한 답을 자산 관리와 연관시켜서 대답해줘."
    },
    // 유저 자산 현황
};
```

```c
Message system_messages[] = {
    {
        "system",
        "Please provide a plain text response without any formatting or emphasis. \
        Use simple language and avoid idiomatic expressions. \
        And answer by Korean."
    },
    {
        "system",
        "Rules:\
        1. Provide only the final answer.\
        2. Do not provide any external website link."
    },
    {
        "system",
        "Steps:\
        1. Answer the user's question.\
        2. Add an answer that can help user manage his assets in that situation."
    },
    // 유저 자산 현황
};

```

https://docs.perplexity.ai/guides/prompt-guide

https://docs.perplexity.ai/api-reference/chat-completions

# 남은 작업

- 콘솔 출력 ui 친근하게
- 매도할 때 현재 보유 주식 보여주기
- 5번 → 저장 완료되었습니다 출력, 링크 띄워주기

## 콘솔 UI/UX

```c
-----------------------------------------
[🔐 로그인]
-----------------------------------------
아이디를 입력하세요: user01
비밀번호를 입력하세요: ********

▶ 로그인 중...
--
╔════════════════════════╗
║       🔐 LOGIN        ║
╠════════════════════════╣
║ ID       :             ║
║ PASSWORD :             ║
╚════════════════════════╝

--
🎉 로그인 성공! 환영합니다, user01님!

(아무 키나 누르면 메인 메뉴로 돌아갑니다)
--
❌ 로그인 실패: 아이디 또는 비밀번호가 일치하지 않습니다.
(아무 키나 누르면 로그인 화면으로 돌아갑니다)

```

## 요구사항 명세서

- 사용자 인증
    - 로그인
- 자산 관리
    - 자산 현황 조회
        - 현금
        - 주식
    - 자산 등록 및 수정
        - 현금
            - 입금
            - 출금
        - 주식
            - 매수
            - 매도
    - 자산 현황 파일로 추출
- 챗봇
    - 챗봇과 대화 - 자산관리 관련으로 물어볼 수 있음
    - 챗봇 대화 내역 파일로 추출

아래 요구사항 정의서를 참고하여 프로젝트의 요구사항을 명확히 정리했습니다.

| 요구사항ID | 요구사항 명 | 구분 | 요구사항 설명 | 요구사항 출처 | 제약 사항 | 활용도 | 해결 방안 | 검수 기준 | 비고 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| UR-01 | 사용자 로그인 | 기능 | 사용자는 등록된 계정(ID/PW)으로 로그인할 수 있고, 실패 시 오류 메시지 출력 | 사용자 인증 | 비밀번호는 암호화하여 저장 및 처리 | 사용자 본인 인증 및 시스템 접근 필수 | DB에 저장된 암호화된 비밀번호 비교 및 인증 처리 | 유효한 ID/PW로 로그인 성공, 잘못된 입력 시 로그인 실패 처리 | 보안 점검 필요 |
| UR-02 | 현금 자산 현황 조회 | 기능 | 사용자는 보유한 현금 자산 내역을 실시간으로 확인 가능 | 자산 관리(현금) | DB와의 실시간 연동 필요 | 사용자 현금 자산 파악 | OCI 통해 DB에서 현금 자산 정보 조회 | DB 내 정보와 일치 여부 | 실시간 조회 성능 확인 필요 |
| UR-03 | 주식 자산 현황 조회 | 기능 | 사용자는 보유한 주식 자산 내역을 실시간으로 확인 가능 | 자산 관리(주식) | DB와의 실시간 연동 필요 | 사용자 주식 자산 파악 | OCI 통해 DB에서 주식 자산 정보 조회 | DB 내 정보와 일치 여부 | 실시간 조회 성능 확인 필요 |
| UR-04 | 현금 자산 입금 | 기능 | 사용자는 현금 자산의 금액을 입금할 수 있으며 자산 정보가 즉시 업데이트 | 자산 관리(현금 입금) | 입금 금액은 양수여야 함 | 현금 자산 증가 관리 | DB에 OCI 연동을 통해 금액 업데이트 처리 | 입금 후 DB 금액 정확히 증가 | 음수 및 문자 처리 유효성 검사 필요 |
| UR-05 | 현금 자산 출금 | 기능 | 사용자는 현금 자산의 금액을 출금할 수 있으며 자산 정보가 즉시 업데이트 | 자산 관리(현금 출금) | 출금 금액이 보유 금액 초과 시 거래 불가 | 현금 자산 감소 관리 | DB에 OCI 연동을 통해 금액 업데이트 처리 | 출금 후 DB 금액 정확히 감소 | 잔액 초과 처리 로직 구현 필요 |
| UR-06 | 주식 매수 | 기능 | 사용자는 특정 종목을 매수할 수 있으며 주식 자산 및 현금 자산이 즉시 업데이트 | 자산 관리(주식 매수) | 보유 현금 부족 시 매수 불가, 매수 후 DB 즉시 반영 | 주식 자산 증가 관리 | OCI 연동으로 DB 내 주식 및 현금 자산 업데이트 | 매수 후 주식 수량 증가 및 현금 감소 정확성 확인 | 보유 현금 검증 필수 |
| UR-07 | 주식 매도 | 기능 | 사용자는 보유한 주식을 매도할 수 있으며 주식 자산 및 현금 자산이 즉시 업데이트 | 자산 관리(주식 매도) | 보유 주식 수량 부족 시 매도 불가, 매도 후 DB 즉시 반영 | 주식 자산 감소 관리 | OCI 연동으로 DB 내 주식 및 현금 자산 업데이트 | 매도 후 주식 수량 감소 및 현금 증가 정확성 확인 | 보유 주식 수량 검증 필수 |
| UR-08 | 자산 현황 파일 추출 | 기능 | 사용자의 전체 자산 현황(현금, 주식)을 파일로 내보낼 수 있음 | 자산 관리(파일 추출) | 파일 포맷은 CSV 형식 | 사용자 자산 기록 및 관리 | 자산 정보를 CSV 파일로 변환하여 저장 | 생성된 CSV 파일 내 자산 정보 DB와 일치 여부 | CSV 포맷 정확성 점검 필요 |
| UR-09 | 챗봇과 대화 | 기능 | 사용자는 챗봇과 대화를 통해 자산 관리 관련 정보를 문의할 수 있음 | 챗봇 기능 | 대화 내용은 DB에 저장됨 | 사용자 문의사항 처리 및 자산 관리 안내 제공 | 챗봇과 사용자 간 대화 기록 저장 및 관리 (CLOB) | 챗봇 응답 정확성, 대화 저장 여부 확인 | 응답 정확도 검토 필요 |
| UR-10 | 챗봇 대화 내역 파일 추출 | 기능 | 사용자는 챗봇과의 대화 내역을 파일로 내보낼 수 있음 | 챗봇 기능(파일 추출) | 파일 포맷은 CSV 형식 | 사용자 챗봇 기록 관리 | 챗봇 대화를 CSV 파일로 변환하여 저장 | 생성된 CSV 파일 내용 DB 내 대화 내역과 일치 여부 | CSV 포맷 정확성 점검 필요 |

# 월요일에 할 것들

- 전체적인 용어, 말투 수정
    
    → 친절하게 - 
    

- 선택 cmd 디자인

- 프로젝트 이름 정하기

- 문서작업
    - 요구사항 정의서
    - 사용자 지침서

- 에러 잡기 (가장 먼저)
