# 필요 DB 함수

- 자산 조회
    - 주식
        - User_Stock *db_getUserStock(const char *user_id, const char *stock_name)
            
            → 못찾으면 NULL 반환
            
- 자산 수정
    - 현금
        - updateAsset(char *user_id, int category, int amount)
    - 주식
        - updateAsset(char *user_id, int category, int amount)
            
            
        - updateUserStock(char* user_id, char* stock_name,  int quantity, double price)
            
            파는 경우에는 price 부호 minus
            
    

## Stock 테이블 변경

- PRICE 추가
    - API로 현재가 받아온다는 설정

# 챗봇 질문 프롬프트

- Please provide a plain text response without any formatting or emphasis. Use simple language and avoid idiomatic expressions. And answer by Korean.
- 너는 지금부터 고객 자금 관리 시스템에서 상담원 역할을 할거야.
- 유저의 현금 자산은 ~~ 있고, 주식 자산은 총 ~~ 있어.
- 유저의 질문에 대한 답을 자산 관리와 연관시켜서 대답해줘.

`유저가 입력한 내용 ~~~`

# 문제

- [ ]  db.c에 get 함수들 사용하면 ‘Error: 000000F0D29FF4B0’ 출력되는 현상
- [x]  DB에서 데이터 받아올 때 문자열 ???로 되는 현상
