```
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

struct student1 {
    char name[13];
    int age;
    char phone[12];
};

struct student2 {
    char name[13];
    int age;
    char phone[12];
} st1, st2;

typedef struct student3 {
    char name[13];
    int age;
    char phone[12];
} ST3;

void pr_10_1_1() {
    typedef struct s_st {
        char name[20];
        char id[20];
        int stid;
        int sub1;
        int sub2;
        int sub3;
        double avg;
    } st;
    st s1, s2;

    printf("학생 1의 정보 입력:\n");
    printf("이름: ");
    scanf("%s", s1.name);
    printf("학번: ");
    scanf("%s", s1.id);
    printf("과목 1 성적: ");
    scanf("%d", &s1.sub1);
    printf("과목 2 성적: ");
    scanf("%d", &s1.sub2);
    printf("과목 3 성적: ");
    scanf("%d", &s1.sub3);
    s1.avg = (double)(s1.sub1 + s1.sub2 + s1.sub3) / 3.0;

    printf("학생 2의 정보 입력:\n");
    printf("이름: ");
    scanf("%s", s2.name);
    printf("학번: ");
    scanf("%s", s2.id);
    printf("과목 1 성적: ");
    scanf("%d", &s2.sub1);
    printf("과목 2 성적: ");
    scanf("%d", &s2.sub2);
    printf("과목 3 성적: ");
    scanf("%d", &s2.sub3);
    s2.avg = (double)(s2.sub1 + s2.sub2 + s2.sub3) / 3.0;

    printf("학생 성적 정보: \n");
    printf("이름: %s, 학번: %s, 평균: %.1f\n", s1.name, s1.id, s1.avg);
    printf("이름: %s, 학번: %s, 평균: %.1f\n", s2.name, s2.id, s2.avg);
}

void pr_10_1_2() {
    typedef struct s_books {
        char name[30];
        char author[20];
        int year;
        char genre[20];
    } t_books;

    t_books daou_books[2] = {{"Data Structures", "Mark", 2022, "알고리즘"}, \
                            {"Clean Code", "Mark", 2022, "프로그래밍"}};
    char choice[20];
    printf("장르를 입력하세요: ");
    scanf("%s", choice);
    printf("'%s' 장르의 책들: \n", choice);
    for (int i = 0; i < sizeof(daou_books); i++) {
        if (strcmp(choice, daou_books[i].genre) == 0)
            printf("제목: %s, 저자: %s, 출판년도: %d, 장르: %s\n", \
            daou_books[i].name, daou_books[i].author, daou_books[i].year, daou_books[i].genre);
    }
}
```
