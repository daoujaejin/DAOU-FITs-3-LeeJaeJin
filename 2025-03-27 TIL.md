# 문제

- [x]  db.c에 get 함수들 사용하면 ‘Error: 000000F0D29FF4B0’ 출력되는 현상
- [x]  DB에서 데이터 받아올 때 문자열 ???로 되는 현상
- [ ]  oracle database connected successfully 출력되는 현상
- [ ]  챗봇 대화 들어가면 `질문을 입력해주세요` 가 두 번 나옴

### 신생함수

- Asset amount 수정
    
    `int db_updateAsset(int asset_id, double delta);` - 테스트 완
    
    `asset_id` - 자산 id로 특정 가능함
    
    `delta` - 변동 분
    
    ```c
    printf("%s) 입금할 금액을 알려주세요!\n>>", g_chatbot_name);
    scanf("%lf", &amount);
    double current_cash = db_getUserAsset(g_user_data->user_id)[IDX_CASH].amount;
    printf("%s) 현재 현금 잔액: %.2f원\n", g_user_data->name, current_cash);
    db_updateAsset(db_getUserAsset(g_user_data->user_id)[IDX_CASH].asset_id, amount);
    double deposit_cash = db_getUserAsset(g_user_data->user_id)[IDX_CASH].amount;
    printf("%s) 입금 후 잔액: %.2f원\n", g_user_data->name, deposit_cash);
    
    ```
    
    - 현금
    - 주식
- UserStock 수정
    
    `void db_updateUserStock(const char* user_id, const char* stock_name, int d_quantity, double d_totalprice)`
    
    파는 경우에는 quantity/price 부호 minus
    
    <aside>
    🤖
    
    **보유하지 않은 주식이 insert로 안넘어가는 문제** 
    
    보유하고 잇는 주식은 문제 없음
    
    </aside>
    
    - 가지고 있지 않은 주식이라면(매수 기준)
        
        `db_insertUserStock(const char* user_id, const char* stock_name, int quantity, double total_price)`
        
        - insert `USER_STOCK`
            - stock_id 받아오기
            - 주식 검증은 재진님이 하시니까 상관 없을듯.
        
        ```
        USER_ID       VARCHAR2(255),
        STOCK_ID      NUMBER,
        QUANTITY      NUMBER,
        TOTAL_PRICE   NUMBER,
        
        ```
        

- STOCK 테이블에 해당 stock_name을 가진 데이터가 있는지 true/false로 반환해주는 함수
    - bool db_checkStockName(char *stock_name); - 테스트 완
        
        ```c
        
            if (db_checkStockName("삼성전자")) {
                printf("있음\n");
            } else {
                printf("없음!\n");
            }
            if (db_checkStockName("다우다우핏츠핏츠")) {
                printf("있음\n");
            } else {
                printf("없음!\n");
            }
        
        ```

        ![스크린샷 2025-03-27 172348](https://github.com/user-attachments/assets/5345cbac-ac1d-4157-855c-c0faeefdf215)
![스크린샷 2025-03-27 171848](https://github.com/user-attachments/assets/c4709d0c-0dff-4af4-8246-fc67637349ec)
![스크린샷 2025-03-27 165407](https://github.com/user-attachments/assets/9d32acfc-923b-47e9-b8c4-0820fc3ac3a8)
![스크린샷 2025-03-27 163430](https://github.com/user-attachments/assets/8f4c7f20-64db-403f-9781-2fa5cf9287af)
![스크린샷 2025-03-27 172553](https://github.com/user-attachments/assets/f38aa3a1-b443-4e1d-96e5-8f3199bfc7d5)
