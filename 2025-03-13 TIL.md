```
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void pr_9_3_3() {
    int arr[3][3];
    printf("3x3 행렬 입력: \n");
    for (int i = 0; i < 3; i++) 
        for (int j = 0; j < 3; j++)
            scanf_s("%d", *(arr + i) + j);
    printf("90도 회전된 행렬:\n");
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++)
            printf("%d ", *(*(arr + j) + (3 - 1 - i))); 
        printf("\n");
    }  
}

void get_len(char *str) {printf("길이: %d\n", strlen(str));}
void conv_to_cap(char *str) {
    for (int i = 0; i < strlen(str); i++) {
        if (str[i] >= 'a' && str[i] <= 'z')
            str[i] -= 32;
    }
}
void conv_to_small(char *str) {
    for (int i = 0; i < strlen(str); i++) {
        if (str[i] >= 'A' && str[i] <= 'Z')
            str[i] += 32;
    }
}

void pr_9_5_2() {
    int choice;
    char str[20];
    void (*func) (char *);
    printf("1. 문자열 길이 구하기\n");
    printf("2. 대문자로 변환하기\n");
    printf("3. 소문자로 변환하기\n");
    printf("선택: ");
    scanf_s("%d", &choice);
    printf("변환할 문자열 입력: ");
    scanf_s("%s", str, 20);
    if (choice == 1) {
        func = get_len;
    } else if (choice == 2) {
        func = conv_to_cap;
    } else if (choice == 3) {
        func = conv_to_small;
    }
    func(str);
    printf("결과: %s\n", str);
}

int compare_ascending(const void* a, const void* b) {
    int* x = (int*)a;
    int* y = (int*)b;
    return *x - *y;
}

int compare_descending(const void* a, const void* b) {
    int* x = (int*)a;
    int* y = (int*)b;
    return *y - *x;
}

void sort_array(int* array, int n, int (*compare)(const void*, const void*)) {
    qsort(array, n, sizeof(int), compare);
}

void pr_9_6_3() {
    int arr[5];
    int choice;
    printf("배열을 입력하세요 (5개의 정수): ");
    scanf_s("%d %d %d %d %d", arr, arr+1, arr+2, arr+3, arr+4);
    printf("1. 오름차순\n");
    printf("2. 내림차순\n");
    printf("선택: ");
    scanf_s("%d", &choice);
    if (choice == 1) {
        sort_array(arr, 5, compare_ascending);
    } else if (choice == 2) {
        sort_array(arr, 5, compare_descending);
    }
    printf("정렬된 배열: %d %d %d %d %d\n", arr[0], arr[1], arr[2], arr[3], arr[4]);
}

void pr_91_1_1() {
    int *arr[5];
    int ar0[4] = {0, 1, 2, 3};
    int ar1[4] = {10, 11, 12, 13};
    int ar2[4] = {20, 21, 22, 23};

    arr[0] = ar0;
    arr[1] = ar1;
    arr[2] = ar2;

    int choice;
    printf("삭제하려는 행의 인덱스 번호를 입력하세요: ");
    scanf_s("%d", &choice);
    arr[choice] = NULL;
    for (int i = 0; i < 3; i++) {
        if (arr[i] != NULL) {
            for (int j = 0; j < 4; j++)
                printf("%d ", arr[i][j]);
            printf("\n");
        }
    }
}

void get_sum(int **arr) {
    int sum = 0;

    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 3; j++)
            sum += arr[i][j];
    printf("배열의 합은 %d\n", sum);
}

void get_max(int **arr) {
    int max = -1;    

    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 3; j++) 
            if (max < arr[i][j])
                max = arr[i][j];
    printf("배열의 최대값은 %d\n", max);
}

void get_min(int **arr) {
    int min = 10;    

    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 3; j++) 
            if (min > arr[i][j])
                min = arr[i][j];
    printf("배열의 최소값은 %d\n", min);
    
}

void get_square(int **arr) {
    printf("배열의 제곱값은\n");
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            printf("%d ", arr[i][j]);
        }
        printf("\n");
    }    
}

void pr_91_2_1() {
    void (*funcs[4])(int**) = {get_sum, get_max, get_min, get_square};
    int array[3][3] = {
        {0, 1, 2},
        {3, 4, 5},
        {6, 7, 8}
    };
    int *ptrs[3];
    ptrs[0] = array[0];
    ptrs[1] = array[1];
    ptrs[2] = array[2];
    int choice;
    printf("연산 방법을 선택하기\n");
    printf("(0: 합, 1: 최대값, 2: 최소값, 3: 제곱): ");
    scanf_s("%d", &choice);
    funcs[choice](ptrs);
}
```
