```
#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>
#define SIZE 5

void pr_9_1_1() {
    printf("========포인터1번========\n");
    int balance[5] = {1000, 5000, 3000, 7000, 2000};
    printf("현제 계좌 잔액: %d, %d, %d, %d, %d\n", balance[0], balance[1], balance[2], balance[3], balance[4]);
    *balance += 500;
    *(balance + 1) += 1000;
    *(balance + 2) += 2000;
    *(balance + 3) += 500;
    *(balance + 4) += 1000;
    printf("업데이트된 계좌 잔액: %d, %d, %d, %d, %d\n", balance[0], balance[1], balance[2], balance[3], balance[4]);
}

void pr_9_1_2() {
    printf("========포인터2번========\n");
    int accounts[SIZE] = {101, 102, 103, 104, 105}; // 계좌번호
    int passwords[SIZE] = {1234, 4321, 5678, 8765, 3456}; // 비밀번호

    int id, pw;
    bool login = false;
    printf("계좌번호 입력: ");
    scanf_s("%d", &id);
    printf("비밀번호 입력: ");
    scanf_s("%d", &pw);
    for (int i = 0; i < SIZE; i++) {
        if (*(accounts + i) == id && *(passwords + i) == pw) {
            printf("로그인 성공!\n");
            return;
        }
    }
    printf("비밀번호 오류!\n");
}

void pr_9_2_3() {
    printf("========포인터3번========\n");
    float scores[5][5];
    for (int i = 0; i < 5; i++) {
        printf("학생 %d 성적 입력: ", i+1);
        scanf_s("%f %f %f %f %f", scores[i], scores[i]+1, \
                    scores[i]+2, scores[i]+3, scores[i]+4);
    }
    printf("학생들의 평균 성적:\n");
    int max = 0, res;
    for (int i = 0; i < 5; i++) {
        float avg = *(scores[i]+0) + *(scores[i]+1) + \
                    *(scores[i]+2) + *(scores[i]+3) + *(scores[i]+4);
        avg /= 5;
        printf("학생 %d 평균: %.1f\n", i+1, avg);
        if (max < avg) {
            max = avg;
            res = i + 1;
        }
    }
    printf("가장 높은 평균을 가진 학생은 학생 %d입니다.\n", res);
}

void pr_9_3_1() {
    int arr[3][3];
    printf("3x3 행렬 입력: \n");
    for (int i = 0; i < 3; i++) 
        for (int j = 0; j < 3; j++)
            scanf_s("%d", *(arr + i) + j);
    for (int i = 0; i < 3; i++) 
        for (int j = 0; j < 3; j++)
            *(*(arr + i) + j) = *(*(arr + i) + j) * *(*(arr + i) + j);
    printf("제곱된 행렬:\n");
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++)
            printf("%d ", *(*(arr + i) + j)); 
        printf("\n");
    }  
    printf("대각선 반전된 행렬 (전치 행렬):\n");
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++)
            printf("%d ", *(*(arr + j) + i)); 
        printf("\n");
    }  
}

void swap(char *a, char *b) {
    char temp[20];
    strcpy(temp, a);
    strcpy(a, b);
    strcpy(b, temp);
}

void pr_9_3_2() {
    char str[3][20];
    printf("문자열 3개 입력:\n");
    for (int i = 0; i < 3; i++)
        scanf_s("%s", *(str + i), 20);
    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 20; j++)
            if (str[i][j] >= 'a' && str[i][j] <= 'z')
                str[i][j] -= 32;
    printf("대문자로 변환된 문자열:\n");
    for (int i = 0; i < 3; i++)
        printf("%s\n", str[i]);
    // 문자열 길이로 정렬
    for (int i = 0; i < 3; i++) {
        for (int j = i + 1; j < 3; j++) {
            if (strlen(str[i]) > strlen(str[j])) {
                swap(str[i], str[j]);
            }
        }
    }
    printf("\n문자열 길이순으로 정렬된 출력:\n");
    for (int i = 0; i < 3; i++) {
        printf("%s (길이: %d)\n", str[i], strlen(str[i]));
    }
}

void pr_9_3_3() {
    int arr[3][3];
    printf("3x3 행렬 입력: \n");
    for (int i = 0; i < 3; i++) 
        for (int j = 0; j < 3; j++)
            scanf_s("%d", *(arr + i) + j);
    printf("90도 회전된 행렬:\n");
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++)
            printf("%d ", *(*(arr + j) + (3 - 1 - i))); 
        printf("\n");
    }  
}

float add(int x, int y) {return x + y;}
float min(int x, int y) {return x - y;}
float mul(int x, int y) {return x * y;}
float divi(int x, int y) {return (float)x / y;}

void pr_9_5_1() {
    int choice;
    int x, y;
    float (*func) (int, int);
    printf("1. 덧셈\n2. 뺄셈\n3. 곱셈\n4. 나눗셈\n");
    printf("선택:");
    scanf_s("%d", &choice);
    printf("두 숫자를 입력하세요: ");
    scanf_s("%d %d", &x, &y);
    printf("결과: ");
    if (choice == 1) {
        func = add;
    } else if (choice == 2) {
        func = min;
    } else if (choice == 3) {
        func = mul;
    } else if (choice == 4) {
        func = divi;
    } 
    printf("%.1f", func(x, y));
}

void get_len(char *str) {printf("길이: %d\n", strlen(str));}
void conv_to_cap(char *str) {
    for (int i = 0; i < strlen(str); i++) {
        if (str[i] >= 'a' && str[i] <= 'z')
            str[i] -= 32;
    }
}
void conv_to_small(char *str) {
    for (int i = 0; i < strlen(str); i++) {
        if (str[i] >= 'A' && str[i] <= 'Z')
            str[i] += 32;
    }
}


void pr_9_5_2() {
    int choice;
    char str[20];
    void (*func) (char *);
    printf("1. 문자열 길이 구하기\n");
    printf("2. 대문자로 변환하기\n");
    printf("3. 소문자로 변환하기\n");
    printf("선택: ");
    scanf_s("%d", &choice);
    printf("변환할 문자열 입력: ");
    scanf_s("%s", str, 20);
    if (choice == 1) {
        func = get_len;
    } else if (choice == 2) {
        func = conv_to_cap;
    } else if (choice == 3) {
        func = conv_to_small;
    }
    func(str);
    printf("결과: %s\n", str);
}
```
